-- WorkSure reference seed (full demo: cd backend && npm run seed)
-- Categories are defined in database/service-catalog.json (6 majors + sub-features).

SET NAMES utf8mb4;

-- After schema is applied, run: npm run seed (from backend/)
